# Dependances Python pour le script generate_app_icons.py
# Installer avec : pip install -r scripts/requirements.txt
#
# cairosvg : conversion SVG -> PNG (rendu vectoriel haute qualite)
# Pillow   : manipulation d'images (masque circulaire pour ic_launcher_round.png)

cairosvg==2.7.1
Pillow==10.4.0
