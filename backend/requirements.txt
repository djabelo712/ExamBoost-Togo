# ─── ExamBoost Togo — Backend dependencies (scope reduit) ─────────────
# Le minimum pour faire tourner l'API FastAPI + les 3 algos ML
# (IRT 3PL, BKT, SM-2) + JWT + SQLite. La prediction du score utilise
# une heuristique basee sur BKT (pas de XGBoost requis pour la demo).

fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.31
pydantic==2.8.2
pydantic-settings==2.3.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
numpy==1.26.4

# ─── Dependances optionnelles (scripts ML avances, non requises pour /docs) ──
# Pour activer XGBoost / calibration py-irt / tests, decommenter :
# xgboost==2.1.0
# scikit-learn==1.5.1
# pandas==2.2.2
# joblib==1.4.2
# py-irt==0.1.5
# httpx==0.27.0
# pytest==8.3.1
