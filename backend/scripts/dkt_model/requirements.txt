# Python dependencies for the DKT (Deep Knowledge Tracing) module.
#
# Install with:
#     pip install -r requirements.txt
#
# CPU-only PyTorch is sufficient for this module (10k students, ~500k
# interactions, single-layer LSTM with 200 hidden units).

torch==2.3.0
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
matplotlib==3.9.0
onnx==1.16.0
onnxruntime==1.18.0
