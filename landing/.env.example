# Copy to .env.local and adjust as needed.
# Landing page works out of the box without any env vars.

# Optional: where to send a webhook/notification on new beta signup.
# BETA_WEBHOOK_URL=

# Optional: site URL used for canonical metadata (defaults to http://localhost:3000 in dev).
# NEXT_PUBLIC_SITE_URL=https://examboost.tg
